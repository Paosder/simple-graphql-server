overwrite: true
schema:
  - http://localhost:4000:
      headers:
        Authorization: 'Bearer Devtest'
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      contextType: ../types#Context
      useIndexSignature: true